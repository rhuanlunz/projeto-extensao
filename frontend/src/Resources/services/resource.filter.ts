import type { Resource } from "./resource.types";

/**
 * Filtra recursos com base em termos de busca (placeholder).
 */
export function filterResources(resources: Resource[], _searchTerm: string): Resource[] {
  // Implementação futura de filtros
  return resources;
}
